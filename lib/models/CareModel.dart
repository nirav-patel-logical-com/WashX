class CareModel {
  final String image;
  final String title;
  final bool isIncluded;
  final double price;
  final bool isSelected;

  const CareModel({this.image, this.title, this.isIncluded, this.price, this.isSelected});
}