class CareModel {
  final String image;
  final String title;
  final double price;
  final bool isIncluded;
  final bool isSelected;

  const CareModel(
      {this.image, this.title, this.price,this.isIncluded, this.isSelected});
}